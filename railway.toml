[build]
builder = "NIXPACKS"
buildCommand = "bash setup-codespace.sh && npm install --prefix client && npm run build --prefix client"

[deploy]
startCommand = "node apply-production.mjs && node server/index.mjs"
restartPolicyType = "ON_FAILURE"
